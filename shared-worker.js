const connections=[];let masterData=null;let isSyncLocked=!1;let activeGamePort=null;self.onconnect=function(event){const port=event.ports[0];connections.push(port);port.onmessage=function(e){const{type,payload}=e.data;switch(type){case 'INIT_DATA':if(!masterData)masterData=payload;break;case 'UPDATE_DATA':if(!isSyncLocked){masterData=payload;broadcast('SYNC_DATA',masterData,port)} break;case 'GAME_START':isSyncLocked=!0;activeGamePort=port;break;case 'GAME_END':if(activeGamePort===port){isSyncLocked=!1;activeGamePort=null;masterData=payload;broadcast('SYNC_DATA',masterData,port)} break}};port.start()};function broadcast(type,payload,excludePort=null){connections.forEach(port=>{if(port!==excludePort){port.postMessage({type,payload})}})}